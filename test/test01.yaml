apiVersion: v0
profile: default
AudioLibraryLocations:
  - name: Samples
    path: /home/folder/samples
  - name: Dingo
    # this pash should get the last / removed
    path: /path/to/my/dingo/
    ignoreUseCount: true
  - path: /my/unnamed/folder
AudioSettings:
  bufferSize: 1024
